package com.dentaloffice.dental_office_app

import io.flutter.embedding.android.FlutterFragmentActivity

// local_auth requires a FragmentActivity (not the plain FlutterActivity)
// to host its biometric prompt dialog.
class MainActivity : FlutterFragmentActivity()
