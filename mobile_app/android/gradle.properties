org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template. AGP 9's
# "built-in Kotlin" transition isn't consistently supported across the plugin
# ecosystem yet (some plugins assume it, others still apply the traditional
# kotlin-android plugin and break under it) — pinned AGP back to a stable
# 8.x release in settings.gradle.kts instead of fighting that transition,
# so this flag is irrelevant again and left at its original default.
android.builtInKotlin=false
